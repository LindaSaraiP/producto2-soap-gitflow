# C++

Las versiones SOAP usan `curl` desde consola mediante `popen`, por lo que se requiere tener `curl` instalado.

Compilar:

```bash
g++ main.cpp -o app
./app
```

En Windows:

```bash
g++ main.cpp -o app.exe
app.exe
```

Abrir:

```text
http://localhost:3000/?n=10
```
