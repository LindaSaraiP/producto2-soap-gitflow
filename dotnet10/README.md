# C# / .NET 10

Crear/ejecutar cada proyecto con:

```bash
cd dotnet10/version1_soap
dotnet run
```

Abrir:

```text
http://localhost:5000/?n=10
```

El código usa Minimal API y es compatible con proyectos ASP.NET Core modernos. Si usas .NET 10, cambia `TargetFramework` a `net10.0`.
