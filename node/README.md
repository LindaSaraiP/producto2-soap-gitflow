# Node.js

Requiere Node.js instalado.

## Instalar dependencias

```bash
cd node/version1_soap
npm install
```

Repetir en cada versión que tenga `package.json`.

## Ejecutar

```bash
node server.js
```

Abrir:

```text
http://localhost:3000/?n=10
```
